db.ident=nsi.oracle
db.nsi.oracle.url=${db.nsi.oracle.url}
db.nsi.oracle.username=${db.nsi.oracle.username}
db.nsi.oracle.password=${db.nsi.oracle.password}

db.nsi.oracle.sys.username=${db.nsi.oracle.sys.username}
db.nsi.oracle.sys.password=${db.nsi.oracle.sys.password}
db.nsi.oracle.global.name=${db.nsi.oracle.global.name}

database.nsi.metadata.path=/opt/nsi/database/metadata
