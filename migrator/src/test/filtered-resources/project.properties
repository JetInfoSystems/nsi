db.nsi.url=${db.nsi.url}
db.nsi.username=${db.nsi.username}
db.nsi.password=${db.nsi.password}

db.nsi.sys.username=${db.nsi.sys.username}
db.nsi.sys.password=${db.nsi.sys.password}
db.nsi.global.name=${db.nsi.global.name}

database.nsi.metadata.path=/opt/nsi/database/metadata
